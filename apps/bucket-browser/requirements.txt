flask==3.0.0
werkzeug==3.0.1
# Cloud storage SDKs — one per backend. boto3 for AWS S3, google-cloud-storage for
# GCP Cloud Storage. Both ship in the image; only STORAGE_PROVIDER decides which is
# used at runtime.
boto3==1.34.0
google-cloud-storage==2.18.0
